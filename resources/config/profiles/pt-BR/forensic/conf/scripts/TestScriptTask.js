/*
	Comments
*/

function getName(){
	return "JavascriptTask";
}

function init(confProps, configFolder){

}

function finish(){
    /*
	var query = "tipo:pdf";
    searcher.setQuery(query);
    var ids = searcher.search().getIds();
    var labelId = ipedCase.getMarcadores().newLabel("bookmark");
	ipedCase.getMarcadores().setLabelComment(labelId, "bookmark comment");
    ipedCase.getMarcadores().addLabel(ids, labelId);
	ipedCase.getMarcadores().saveState();
	*/ 
}

function process(item){
	//item.setExtraAttribute("newProperty", "true"); 
}