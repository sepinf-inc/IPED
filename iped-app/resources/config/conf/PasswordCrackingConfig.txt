# Configuration for password cracking (e.g. DPAPI master key cracking performed by the Zoom parser).

# Maximum time, in seconds, spent trying to crack each password hash.
# When the limit is reached, cracking of that item stops and the hashcat-compatible
# hash is reported (in the log and in the item metadata) so it can be cracked offline.
# Use 0 to disable the time limit.
crackTimeoutSeconds = 300

# Log a warning when the dictionary file passed with -dictionary/-dict is larger
# than this size (in MB), as cracking may take a long time.
# Use 0 to disable the warning.
warnDictionarySizeMB = 100

# Number of parallel threads used to try candidate passwords.
# Use a positive integer, or "default" to use half of the available processors (minimum 1).
crackingThreads = default
